<h1>Listing entities</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Amount</th>
    <th>Group</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @entities.each do |entity| %>
  <tr>
    <td><%= entity.name %></td>
    <td><%= entity.amount %></td>
    <td><%= entity.group %></td>
    <td><%= link_to 'Show', [entity.group, entity] %></td>
    <td><%= link_to 'Edit', edit_group_entity_path(entity.group, entity) %></td>
    <td><%= link_to 'Destroy', [entity.group, entity], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Entity', new_group_entity_path(params[:group_id]) %>
